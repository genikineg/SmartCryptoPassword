git init

git add -A

git status

git commit -m "first commit"

git commit -m "new password map generator (the ability to create a personalized card for password generation)"


git remote add github https://github.com/genikineg/SmartCryptoPassword.git
git push github master

git remote add bitbucket https://genikineg@bitbucket.org/genikineg/smartcryptopassword.git
git push bitbucket master

почистить кэш
git rm --cached .idea

